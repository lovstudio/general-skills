# Pricing Cards

每个 catalog Skill 对应一张独立 Pricing Card。`index.yaml` 是内部维护的机器可读索引；`skills.yaml` 只同步经过整理的公开字段，供 LovStudio Skills 页面展示。

卡片会记录交付结果、价格区间、价值锚点、六维评分、使用边界、维护触发和证据缺口。价格变化先更新卡片，再同步 `skills.yaml` 与网站。
